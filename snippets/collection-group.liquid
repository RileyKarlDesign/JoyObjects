{% comment %} takes collection  {% endcomment %}



<div class="md:un-hidden md:grid  hidden grid-cols-4 p-2 gap-2">

    <div class=" ">
            <h3 class=" text-serif ">{{collection.title}}</h3>
            {% comment %} {% render 'button' with { text:' View All' , link: collection.url } %} {% endcomment %}
    </div>

    <div class=" grid md:grid-cols-3 md:col-span-3 grid-cols-2 col-span-2 gap-2 ">
            {% for product in collection.products  %}
                {% render 'product-card' with { product: product }  %}
            {% endfor  %}
    </div>

</div>

<div class="md:hidden un-hidden grid grid-cols-4 p-2 gap-2">

    <div class=" grid md:grid-cols-2 md:col-span-4 grid-cols-1 col-span-4 gap-2 ">
            {% for product in collection.products  %}
                {% render 'product-card' with { product: product }  %}
            {% endfor  %}
    </div>

</div>