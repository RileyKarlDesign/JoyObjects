<div class=" opacity-50  hover:opacity-100 transition-all p-2">

        <div class=''>
                                        
                        <div class='w-full text-md menu-link font-sans flex  items-end gap-2' >
                                {% for link in linklists.footer.links %} 
                                <h2 class=''><a href="{{ link.url }}">{{ link.title }}</a></h2>
                                {% endfor %}
                            
                        </div>

                <div class=' ' >
                 {{  shop.brand.logo | image_url: width: 700 | image_tag }} 
                </div>

        </div>

</div>